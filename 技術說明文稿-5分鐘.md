# TripMate 專案技術說明文稿（5分鐘版）

## 【開場】（30秒）

大家好，今天我要為大家介紹 TripMate 這個智慧旅遊社群平台的技術架構。

TripMate 是一個解決旅行者找旅伴痛點的一站式平台，整合了社群互動、行程規劃、旅伴配對與 AI 助手等功能。我們團隊由五位轉職工程師組成，從零開始打造這個專案。

---

## 【技術架構總覽】（1分鐘）

首先談技術架構。我們採用前後端分離的架構設計。

**前端部分**，我們使用 Vue 3 作為核心框架，搭配 Vite 作為建置工具，提供快速的開發體驗。UI 框架採用 Tailwind CSS，讓我們能夠快速打造響應式且美觀的介面。狀態管理使用 Pinia，這是 Vue 3 官方推薦的狀態管理方案。路由使用 Vue Router，而富文本編輯器則採用 TipTap，讓使用者能夠輕鬆編輯和分享旅遊內容。

**後端部分**，我們使用 Node.js 搭配 Express 5.2 框架來建立 RESTful API。資料庫選擇 PostgreSQL，這是一個功能強大的關聯式資料庫，能夠處理複雜的資料關聯。在身份驗證方面，我們整合 Firebase Authentication，提供安全的登入機制。檔案儲存則使用 Firebase Storage，方便管理使用者上傳的圖片和檔案。

---

## 【核心功能與技術亮點】（2分鐘）

接下來介紹幾個核心功能的技術實作。

**第一個是智慧旅伴配對系統**。我們設計了性格測驗功能，透過演算法分析使用者的旅遊偏好和個性特質，精準推薦適合的旅伴。這個系統會將使用者配對結果儲存在 PostgreSQL 中，並透過前端的 Pinia store 管理配對狀態。

**第二個是即時聊天系統**。我們使用 WebSocket 技術來實現即時通訊功能。後端使用 ws 套件建立 WebSocket 伺服器，前端透過 WebSocket API 連接。當使用者發送訊息時，系統會即時轉發給接收者，如果接收者不在線，訊息會儲存在資料庫中，待使用者上線時再同步載入。我們還實作了心跳檢測機制，確保連線的穩定性，並有自動重連功能，提升使用者體驗。

**第三個是 AI 智慧助手**。我們整合 Google Gemini API，打造專屬的旅遊 AI 助手。這個助手能夠回答旅遊相關問題，提供行程建議，並協助使用者了解網站功能。我們設計了完整的系統提示詞，讓 AI 能夠準確理解 TripMate 的業務規則和功能限制，避免產生錯誤資訊。

**第四個是完整的電商功能**。我們實作了購物車、結帳流程和訂單管理系統。結帳流程分為五個步驟：確認行程、填寫表單、審核資訊、選擇付款方式，最後完成訂單。我們串接了 LINE Pay 和信用卡付款，並設計了完整的訂單狀態追蹤機制。

**第五個是權限管理系統**。我們實作了 RBAC，也就是角色權限控制系統。系統中有三種主要角色：訪客、旅客和商家。不同角色擁有不同的功能權限，例如只有商家可以上架付費行程，只有登入的使用者可以發文和留言。這個權限系統貫穿整個應用程式，確保資料安全和使用者體驗。

---

## 【資料庫設計與 API 架構】（1分鐘）

在資料庫設計方面，我們使用 PostgreSQL 的 Schema 功能來組織資料。主要分為三個 Schema：public 存放核心資料，travelers 存放找旅伴相關資料，discussion 存放討論區資料。這樣的設計讓資料結構更清晰，也方便後續維護。

API 設計採用 RESTful 風格，後端提供多個路由模組，包括討論區、留言、按讚、使用者、行程、購物車、訂單、付款等功能。每個路由模組都實作了完整的 CRUD 操作，並整合了 Firebase 身份驗證中介軟體，確保 API 的安全性。

我們還實作了 CORS 跨域設定，允許前端從不同網域存取 API，並設定了適當的請求大小限制，支援圖片上傳等大檔案操作。

---

## 【前端技術細節】（30秒）

在前端技術方面，我們使用 Composition API 來組織元件邏輯，讓程式碼更模組化和可維護。我們建立了多個 Composables，例如 useChat、useComments、useItinerary 等，將重複使用的邏輯封裝成可重用的函式。

在圖片處理方面，我們實作了圖片裁切和壓縮功能，確保上傳的圖片符合規格且檔案大小適中。我們還使用 vuedraggable 套件來實現行程的拖曳排序功能，讓使用者能夠直觀地調整行程順序。

---

## 【部署與 DevOps】（30秒）

在部署方面，我們使用 Vercel 部署前端，使用 Zeabur 部署後端服務。資料庫使用 Neon 提供的 PostgreSQL 雲端服務，這是一個 Serverless 資料庫，能夠自動擴展並提供連接池功能。

我們還設定了環境變數管理，將敏感資訊如 API 金鑰、資料庫連線字串等存放在環境變數中，確保安全性。後端資料庫連線實作了重試機制和錯誤處理，提升系統的穩定性。

---

## 【結語】（30秒）

總結來說，TripMate 是一個技術棧完整、功能豐富的現代化 Web 應用程式。我們使用了 Vue 3、Express、PostgreSQL、Firebase 等主流技術，並整合了 AI 和 WebSocket 等進階功能。整個專案展現了我們團隊在 Full-Stack 開發、資料庫設計、API 設計和系統架構規劃方面的能力。

專案目前已經上線，網址是 tripmate-mayoyo.com，歡迎大家體驗。謝謝大家！

---

## 【備註】

- 總字數：約 1,200 字
- 口述時間：約 5 分鐘（以每分鐘 240 字計算）
- 建議語速：適中，重點部分可稍作停頓
- 可根據實際情況調整各部分時間分配




