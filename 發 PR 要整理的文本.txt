標題：修正留言按讚無法保存

做了什麼：
- 新增留言按讚/取消按讚 API，寫回留言的 likes_count
- 前端留言按讚改為呼叫 API，失敗時回復本地狀態

怎麼做：
- 後端在 `comments` 路由新增留言按讚端點，使用 UPDATE + GREATEST 保護數值
- 前端在留言按讚時帶 action=like/unlike，並以回傳值同步 likes

改了哪些檔案：
- `backend/routes/comments.js`
- `frontend/src/api/comments.js`
- `frontend/src/components/modals/DiscussionDetailModal.vue`
- `frontend/src/components/modals/TravelerDetailModal.vue`

測試：
- 未執行（需要時可補）

---
標題：性格測驗結果同步保存 Firebase 與 Neon

做了什麼：
- 儲存性格測驗結果時，同步更新 Neon（users.spirit_animal）與 Firestore（users.spiritAnimal）

怎麼做：
- 在 personality store 的 saveResult 裡補上 Firestore 的 update/set 流程

改了哪些檔案：
- `frontend/src/stores/personality.js`

測試：
- 未執行（需要時可補）

---
標題：修正更新使用者資料參數錯位導致 500

做了什麼：
- 修正 users 更新 SQL 參數組裝，避免未提供 avatar 時參數位移

怎麼做：
- 用 addParam 統一推進參數索引，只有真的使用參數才加索引

改了哪些檔案：
- `backend/routes/users.js`

測試：
- 未執行（需要時可補）

---
標題：補齊 users 更新參數型別轉型避免型別推斷錯誤

做了什麼：
- 在 users 更新 SQL 加上型別轉型，避免 null/空陣列導致型別推斷失敗

怎麼做：
- 對 nickname/location/avatar/bio/spirit_animal/tags 加上 ::text 或 ::text[] 轉型

改了哪些檔案：
- `backend/routes/users.js`

測試：
- 未執行（需要時可補）

---
標題：只更新有提供的欄位，避免未定義參數導致 500

做了什麼：
- users 更新只在欄位有提供時才加入 SQL 參數
- 移除多餘更新 debug log

怎麼做：
- 條件加入 nickname/location/avatar/bio/spirit_animal/tags

改了哪些檔案：
- `backend/routes/users.js`

測試：
- 未執行（需要時可補）

